import 'package:flutter/material.dart';

class HomeView extends StatelessWigdet {
  const HomeView({Key? key}) : super(key: key);

  @override
  Wigdet build(BuildContext context) {
    return Scaffold();
  }
}
